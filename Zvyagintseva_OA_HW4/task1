"""Вычислить число π c заданной точностью d
Примечание! В Python число π имеет максимум 15 символов после точки."""

from math import pi

d = input('Сколько знаков после точке нужно вывести в числе π (до 15 символов после точки):  ')
while not d.isdigit() or int(d) > 15:
    d = input('Сколько знаков после точке нужно вывести в числе π (до 15 символов после точки):  ')

if int(d) == 0:
    print(3.0)
else:
    print(str(pi)[:int(d)+2])
