# Напишите программу, удаляющую из текста все слова, содержащие ""абв"".

# lambda (lambda уже использовалась в старом решении, но довела до ума)
my_text = 'Просилабв Аабвня ожерельеабв ноабв не расслышал Дед Мороз и ожирение в подарок принес'.split()
print(' '.join([word for word in my_text if 'абв' not in word]))

# Старый вариант
# my_text = 'Просилабв Аабвня ожерельеабв ноабв не расслышал Дед Мороз и ожирение в подарок принес'
# find_txt = "абв"
# result = ' '.join(filter(lambda x: 'абв' not in x, my_text.split()))
# print(result)
